SELECT people.*, COUNT(sales.sale) AS sale_count,
RANK() OVER (PARTITION BY people.id ORDER BY people.id DESC) AS sale_rank
FROM people
JOIN sales
  ON people.id = sales.people_id
GROUP BY people.id;
